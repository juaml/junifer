Add ``space`` parameter to :func:`.register_coordinates`, :func:`.register_parcellation` and :func:`.register_mask`; return space from :func:`.load_coordinates`, :func:`.load_parcellation` and :func:`.load_mask`; modify ``preprocessor`` parameter to ``preprocessors`` in :func:`.run` to accept multiple preprocessors; modify ``preprocessing`` parameter to ``preprocessors`` in :class:`.MarkerCollection` by `Synchon Mandal`_ and `Fede Raimondo`_
