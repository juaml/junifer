Add ``space`` information to existing datagrabbers, masks, parcellations and coordinates; extend Docker support to FSL to allow FSL commands to be called by ``junifer``; improve :class:`.BasePreprocessor` and :class:`.fMRIPrepConfoundRemover` by `Synchon Mandal`_ and `Fede Raimondo`_
